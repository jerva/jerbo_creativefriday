/*global describe, it, assert, code */
(function() {
	"use strict";
	//var assert = require('chai').assert;
		
	describe("Example", function () {
		it("should run", function () {
			assert.equal(code.foo(), 'foo','foo equal `bar`');
		});
	});
}());